name: AUR Launcher

on:
  workflow_dispatch:
  schedule:
    - cron: "0 3 * * *"

jobs:
  build:
    strategy:
      matrix:
        package: [quickshell-git]
      max-parallel: 1
    uses: ./.github/workflows/builder.yml
    with:
      package_name: ${{ matrix.package }}
